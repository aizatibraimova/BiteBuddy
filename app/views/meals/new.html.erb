<h1>New Meal</h1>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-12">
      <%= form_with(model: [@child, @meal], local: true) do |form| %>
        <div class="card">
          <div class="card-header">New Meal</div>
          <div class="card-body">
            <%= render 'form', form: form %>
          </div>
          <div class="card-footer">
            <%= form.submit 'Create Meal', class: 'btn btn-primary' %>
            <%= link_to 'Back to Details', details_child_path(@child), class: 'btn btn-secondary' %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
